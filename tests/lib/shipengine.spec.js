// import ShipEngine from '../../lib/shipengine.js';

import assert from 'assert';

describe('shipengine', () => {
	describe('basic functionality', () => {
		it('should pass', () => {
			assert.equal(1, 1);
		});
	});
});
